import { generateNonce } from "@meshsdk/core";

async function backendGetNonce(userStakeAddress: string) {
  // do: if new user, create new user model in the database

  // do: store 'nonce' in user model in the database

  
  // do: return 'nonce'
}
